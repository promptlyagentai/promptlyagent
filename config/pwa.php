<?php

return [

    /*
    |--------------------------------------------------------------------------
    | PWA Icon Version
    |--------------------------------------------------------------------------
    |
    | Cache-busting version for PWA icons. Increment this value whenever you
    | update PWA icons to force iOS and other platforms to refresh cached
    | icons. After changing icons, increment this value and users will see
    | the new icons after reinstalling the PWA.
    |
    | Example: Change from '1' to '2' after updating icon files.
    |
    */

    'icon_version' => env('PWA_ICON_VERSION', '2'),

];
